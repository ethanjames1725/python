"""This code is intentionally incorrect to demonstrate error handling
in Python."""

# list_of_numbers = [1, 2, 3, 4, 5]
# print(list_of_numbers[10])  # This will raise an IndexError since index
# 10 does not exist in the list.
# remove_item = list_of_numbers.remove(6)  # This will raise a ValueError
# since 6 is not in the list.
# print(remove_item)  # This will print None since the remove() method does
# not return anything.

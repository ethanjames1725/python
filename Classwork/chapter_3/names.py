# Summary: This code creates a list of names and uses a for loop to iterate through the list, 
# printing a personalized greeting for each name. The f-string is used to format the output, 
# allowing the name to be included in the greeting message.
names = ['Alice', 'Bob', 'Charlie', 'David', 'Eve']
for name in names:
    print(f"Hello, {name}!")